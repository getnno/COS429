def check_layers():
    """Placeholder for staff/reference layer checks.

    The student starter bundle does not include hidden reference
    implementations. Use the course autograder for authoritative checks.
    """
    raise RuntimeError(
        "Reference layer checks are not included in the student starter bundle.")


if __name__ == "__main__":
    check_layers()
